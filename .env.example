# ========================================
# Backend Environment Variables
# ========================================

# Server port (optional - defaults to 3000)
PORT=3000

# MongoDB connection string
# Format: mongodb://host:port/database
# For Docker: use service name 'mongo'
# For local dev: use 'localhost'
MONGODB_URI=mongodb://mongo:27017/dagboken

# JWT secret for authentication tokens
# Generate a secure random string for production
# Example: openssl rand -base64 32
JWT_SECRET=change_me_in_production_use_secure_random_string

# Gemini API key for external service integration
GEMINI_API_KEY=your_gemini_api_key_here

# ========================================
# Frontend Environment Variables
# ========================================

# Backend API base URL
# For Docker: http://localhost:3000 (browser calls host)
# For local dev: http://localhost:3000
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# ========================================
# Notes
# ========================================
# 1. Copy this file to .env and fill in real values
# 2. Never commit .env to version control
# 3. For production, use secure random strings for JWT_SECRET
# 4. NEXT_PUBLIC_* variables are exposed to the browser
